includes: ['layer:basic', 'interface:jenkins-extension', 'interface:cwr-ci']
repo: https://github.com/juju-solutions/layer-cwr.git
options:
  basic:
    packages:
      - unzip
      - jq
    use_venv: true
