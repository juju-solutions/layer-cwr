includes: ['layer:basic', 'layer:apt', 'interface:jenkins-extension', 'interface:cwr-ci']
repo: https://github.com/juju-solutions/layer-cwr.git
options:
  basic:
    use_venv: true
  apt:
    packages:
      - lxd
      - juju
      - python-apport
      - python-yaml
      - jq
      - unzip
      - gpgv2
      - charm-tools
